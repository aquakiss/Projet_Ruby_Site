<%= simple_form_for(@map) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :map_id %>
  </div>

  <%= f.hidden_field(:map_url_cache) %>
  <%= f.file_field(:map_url) %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
